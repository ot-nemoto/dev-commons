# project.md（このリポジトリ固有の情報）

> このファイルは **このリポジトリが所有** する。自由に編集してよく、dev-commons からは同期されない（上書きされない）。
> 共通ルールは `.claude/common-rules.md`（dev-commons が配布）にあり、ルートの `CLAUDE.md` が本ファイルと共に import する。
> 共通ルールと矛盾する記述は、import 順で後ろにある本ファイルの内容が優先される。
>
> ここに書くのは**このリポジトリでしか通用しない情報のみ**（採否・テスト対象・真に固有なルール）。共通ルールや複数リポで共有できる規約は `.claude/common-rules.md` / `.claude/api-rules.md` にあるので重複させない。

## 本リポジトリのドキュメント採否

- **必須ドキュメント**: `product` / `architecture` / `ui` / `development`
- **条件付き必須ドキュメント**: <採用しているものを列挙。API・DB・認証系（api 仕様＝配信 spec / `actions` / `schema` / `auth`）の規約は `.claude/api-rules.md` を正とし、ここでは採否のみ記す。なければ「該当なし」とし理由を一言（例: サーバーレス・クライアント完結の SPA のため該当なし）>
- <その他の一点物 docs（外部データソースのフィールド定義など）を採用する場合は、条件・必須セクションをここに定義する>

## テスト対象（このリポジトリ固有）

- ユニットテスト対象: <例: `src/lib/` / `src/app/utils/` / `src/app/hooks/`>
- API ルート: <有無。ある場合は `app/api/` を対象に含める>

## このリポジトリ固有のルール（任意）

<共通ルール（`.claude/common-rules.md` / `.claude/api-rules.md`）にない、このリポジトリでしか通用しない独自ルールや例外のみ記載する（複数リポで共有できるものは共有レイヤーへ）。不要なら本セクションは削除してよい。>
